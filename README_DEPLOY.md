To deploy the Supabase edge function using your token:

1. In your terminal, run:
   export SUPABASE_ACCESS_TOKEN="your_sbp_token_here"

2. Then run the deploy command:
   npx supabase functions deploy upgrade-agency-role --project-ref ljgifhifmvwmoblpwbth
